{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "1.1.4"
  },
  "dependencies": {
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-router-dom": "^4.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf ../build && react-scripts build && mv ./build ..",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
